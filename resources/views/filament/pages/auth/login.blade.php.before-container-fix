<div class="skytech-login-page">
    <section class="skytech-login-visual" aria-label="SKYTECH INFRANET network access">
        <div class="skytech-login-visual__overlay"></div>
        <div class="skytech-login-brand">
            <img src="{{ asset('images/skytech-infranet-logo.png') }}" alt="SKYTECH INFRANET" class="skytech-login-brand__logo">
        </div>
    </section>

    <main class="skytech-login-form-panel">
        <div class="skytech-login-form-wrap">
            <div class="skytech-login-intro">
                <h1>Sign in to access your administrative workspace and networks dashboard.</h1>
            </div>

            {{ \Filament\Support\Facades\FilamentView::renderHook('panels::auth.login.form.before') }}

            <div class="skytech-login-form">
                {{ $this->getFormContentComponent() }}
            </div>

            {{ \Filament\Support\Facades\FilamentView::renderHook('panels::auth.login.form.after') }}

            <div class="skytech-login-divider" aria-hidden="true">
                <span></span>
                <small>SECURE ADMINISTRATIVE ACCESS</small>
                <span></span>
            </div>

            <p class="skytech-login-security-note">Help Desk: {{ siteUrlSettings('help_desk_phone') ?? '+255622221464/+255754448446' }}<br>
                Authorized SKYTECH INFRANET administrators only. Use HTTPS in production and never share your password.
            </p>
        </div>
    </main>
</div>

<style>
    :root {
        --skytech-navy: #111827;
        --skytech-blue: #0b73c9;
        --skytech-border: #d7deea;
        --skytech-muted: #60708a;
    }

    .fi-simple-layout {
        min-height: 100vh !important;
        padding: 0 !important;
        background: #ffffff !important;
    }

    .fi-simple-main-ctn {
        width: 100% !important;
        max-width: none !important;
        min-height: 100vh !important;
        padding: 0 !important;
        margin: 0 !important;
    }

    .fi-simple-main {
        width: 100% !important;
        max-width: none !important;
        padding: 0 !important;
        margin: 0 !important;
    }

    .skytech-login-page {
        min-height: 100vh;
        display: grid;
        grid-template-columns: minmax(42%, 1fr) minmax(520px, 58%);
        background: #fff;
        overflow: hidden;
    }

    .skytech-login-visual {
        position: relative;
        min-height: 100vh;
        background-image: url('{{ asset('images/skytech-login-network.jpg') }}');
        background-size: cover;
        background-position: center;
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
    }

    .skytech-login-visual__overlay {
        position: absolute;
        inset: 0;
        background: linear-gradient(135deg, rgba(25, 8, 27, .74), rgba(11, 115, 201, .18));
    }

    .skytech-login-brand {
        position: relative;
        z-index: 1;
        width: min(70%, 430px);
        padding: 18px;
        border-radius: 24px;
        background: rgba(255,255,255,.96);
        box-shadow: 0 22px 55px rgba(0,0,0,.28);
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .skytech-login-brand__logo {
        display: block;
        width: 100%;
        height: auto;
        max-height: 430px;
        object-fit: contain;
    }

    .skytech-login-form-panel {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: #ffffff;
        padding: 48px 7vw;
    }

    .skytech-login-form-wrap {
        width: min(100%, 660px);
    }

    .skytech-login-intro {
        margin-bottom: 34px;
    }

    .skytech-login-intro h1 {
        margin: 0;
        color: #314a6b;
        font-size: clamp(1.25rem, 1.7vw, 1.65rem);
        line-height: 1.45;
        font-weight: 400;
        letter-spacing: -.01em;
    }

    .skytech-login-form {
        border: 1px solid var(--skytech-border);
        border-radius: 12px;
        padding: 44px 48px 38px;
        box-shadow: 0 10px 28px rgba(17, 24, 39, .06);
        background: #fff;
    }

    .skytech-login-form .fi-fo-field-wrp-label,
    .skytech-login-form label {
        color: #111827;
        font-weight: 600;
    }

    .skytech-login-form input {
        min-height: 58px;
        border-radius: 10px;
        border-color: #c7d0df;
        background: #fff;
        font-size: 1rem;
    }

    .skytech-login-form input:focus {
        border-color: var(--skytech-blue);
        box-shadow: 0 0 0 3px rgba(11, 115, 201, .13);
    }

    .skytech-login-form button[type="submit"] {
        min-height: 58px;
        border-radius: 10px;
        font-size: 1.05rem;
        font-weight: 600;
    }

    .skytech-login-divider {
        display: grid;
        grid-template-columns: 1fr auto 1fr;
        gap: 16px;
        align-items: center;
        margin: 30px 0 12px;
        color: #8b9ab0;
    }

    .skytech-login-divider span {
        height: 1px;
        background: #dce3ec;
    }

    .skytech-login-divider small {
        font-size: .76rem;
        letter-spacing: .08em;
        white-space: nowrap;
    }

    .skytech-login-security-note {
        margin: 0;
        text-align: center;
        color: var(--skytech-muted);
        font-size: .84rem;
        line-height: 1.55;
    }

    @media (max-width: 900px) {
        .skytech-login-page {
            grid-template-columns: 1fr;
        }

        .skytech-login-visual {
            min-height: 300px;
            max-height: 38vh;
        }

        .skytech-login-brand {
            width: min(42vw, 250px);
            padding: 10px;
        }

        .skytech-login-form-panel {
            min-height: auto;
            padding: 38px 22px 48px;
        }
    }

    @media (max-width: 560px) {
        .skytech-login-form {
            padding: 28px 22px 24px;
        }

        .skytech-login-intro {
            margin-bottom: 24px;
        }
    }
</style>
